const Note = ({ children }) => (
    <div style={{
      backgroundColor: '#FAF5FF',
      borderLeft: '4px solid #7C3AED',
      padding: '12px',
      margin: '16px 0',
      color: '#5B21B6'
    }}>
      {children}
    </div>
  );
  
  export default Note;
  